# =============================================================================
# Boxwatchr Rules
# =============================================================================

# Add your rules below. See rules.yaml.example for full documentation
# on all available fields, operators, and actions.

rules: